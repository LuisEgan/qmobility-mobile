import React from "react";
import { View, StyleSheet } from "react-native";

interface ILinkedIn {}

const LinkedIn = (props: ILinkedIn) => {
  const {} = props;

  return <View style={styles.container}></View>;
};

export default LinkedIn;

const styles = StyleSheet.create({
  container: {},
});
