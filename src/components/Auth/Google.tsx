import React from "react";
import { View, StyleSheet } from "react-native";

interface IGoogle {}

const Google = (props: IGoogle) => {
  const {} = props;

  return <View style={styles.container}></View>;
};

export default Google;

const styles = StyleSheet.create({
  container: {},
});
